引用 模块：标准库-输入输出-输出 为 输出

// ========== 函数与递归测试 ==========

// 有返回值函数
定义 方法 相加（x：整数，y：整数）返回 整数：
    返回 x + y
。。

// 多参数有返回值函数
定义 方法 乘加（a：整数，b：整数，c：整数）返回 整数：
    返回 a * b + c
。。

// 阶乘（递归）
定义 方法 阶乘（n：整数）返回 整数：
    判断 n <= 1：
        返回 1
    。。
    返回 n * 阶乘（n - 1）
。。

// 斐波那契（递归）
定义 方法 斐波那契（n：整数）返回 整数：
    判断 n <= 1：
        返回 n
    。。
    返回 斐波那契（n - 1） + 斐波那契（n - 2）
。。

// 判断闰年
定义 方法 是闰年（年份：整数）返回 整数：
    判断 年份 % 400 == 0：
        返回 1
    。。
    判断 年份 % 100 == 0：
        返回 0
    。。
    判断 年份 % 4 == 0：
        返回 1
    。。
    返回 0
。。

@声明 入口
定义 方法 测试函数（）返回 无：
    // 1. 直接函数调用（括号形式）
    设 r1 = 相加（3，5）
    执行 输出：f"测试1: 相加(3,5)={r1}"

    // 2. 多参数函数
    设 r2 = 乘加（2，3，4）
    执行 输出：f"测试2: 乘加(2,3,4)={r2}"

    // 3. 函数调用作为表达式的一部分
    设 r3 = 相加（1，2） + 相加（3，4）
    执行 输出：f"测试3: 相加(1,2)+相加(3,4)={r3}"

    // 4. 递归-阶乘
    设 r4 = 阶乘（5）
    执行 输出：f"测试4: 阶乘(5)={r4}"

    // 5. 递归-斐波那契
    设 r5 = 斐波那契（7）
    执行 输出：f"测试5: 斐波那契(7)={r5}"

    // 6. 多分支函数
    设 r6 = 是闰年（2024）
    执行 输出：f"测试6: 2024是闰年={r6}"
    设 r7 = 是闰年（2023）
    执行 输出：f"测试6b: 2023是闰年={r7}"

    // 7. 取值 形式调用
    设 r8 = 取值 相加：1，2
    执行 输出：f"测试7: 取值相加(1,2)={r8}"

    // 8. 取值 带类型标注
    设 r9 = 取值 相加->整数：10，20
    执行 输出：f"测试8: 取值相加->整数(10,20)={r9}"

    // 9. 函数调用作为判断条件
    判断 是闰年（2000）：
        执行 输出："测试9: 2000是闰年"
    。。

    // 10. 执行（调用无返回值函数 输出）
    执行 输出："测试10: 执行调用void函数通过"

    // 11. 嵌套函数调用
    设 r10 = 相加（阶乘（3），斐波那契（4））
    执行 输出：f"测试11: 相加(阶乘(3),斐波那契(4))={r10}"

    执行 输出："=== 函数测试通过 ==="
。。

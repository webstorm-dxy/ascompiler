引用 模块：标准库-输入输出-输出 为 输出

// ========== 运算符与表达式测试 ==========

@声明 入口
定义 方法 测试运算符（）返回 无：
    // 1. 加法
    设 a = 10 + 5
    执行 输出：f"测试1: 10+5={a}"

    // 2. 减法
    设 b = 20 - 7
    执行 输出：f"测试2: 20-7={b}"

    // 3. 乘法
    设 c = 6 * 8
    执行 输出：f"测试3: 6*8={c}"

    // 4. 除法
    设 d = 100 / 4
    执行 输出：f"测试4: 100/4={d}"

    // 5. 取余
    设 e = 17 % 5
    执行 输出：f"测试5: 17%5={e}"

    // 6. 小数运算
    设 f = 3.5 + 2.5
    执行 输出：f"测试6: 3.5+2.5={f}"

    // 7. 小数乘除
    设 g = 10.0 / 3.0
    执行 输出：f"测试7: 10.0/3.0={g}"

    // 8. 等于
    定义 变量：布尔 eq = 5 == 5
    判断 eq：
        执行 输出："测试8: 5==5 为真"
    。。

    // 9. 不等于
    定义 变量：布尔 ne = 5 != 3
    判断 ne：
        执行 输出："测试9: 5!=3 为真"
    。。

    // 10. 小于
    定义 变量：布尔 lt = 3 < 10
    判断 lt：
        执行 输出："测试10: 3<10 为真"
    。。

    // 11. 小于等于
    定义 变量：布尔 le = 5 <= 5
    判断 le：
        执行 输出："测试11: 5<=5 为真"
    。。

    // 12. 大于
    定义 变量：布尔 gt = 10 > 3
    判断 gt：
        执行 输出："测试12: 10>3 为真"
    。。

    // 13. 大于等于
    定义 变量：布尔 ge = 5 >= 5
    判断 ge：
        执行 输出："测试13: 5>=5 为真"
    。。

    // 14. 逻辑与 &&
    定义 变量：布尔 and_test = 1 == 1 && 2 == 2
    判断 and_test：
        执行 输出："测试14: 且运算 为真"
    。。

    // 15. 逻辑或 ||
    定义 变量：布尔 or_test = 1 == 2 || 2 == 2
    判断 or_test：
        执行 输出："测试15: 或运算 为真"
    。。

    // 16. 中文且运算符
    定义 变量：布尔 cn_and = 1 == 1 且 2 == 2
    判断 cn_and：
        执行 输出："测试16: 中文且 为真"
    。。

    // 17. 中文或运算符
    定义 变量：布尔 cn_or = 1 == 2 或 2 == 2
    判断 cn_or：
        执行 输出："测试17: 中文或 为真"
    。。

    // 18. 一元取负
    设 h = -5
    执行 输出：f"测试18: -5={h}"

    // 19. 小数取负
    设 i = -3.14
    执行 输出：f"测试19: -3.14={i}"

    // 20. 复合表达式
    设 j = 2 + 3 * 4
    执行 输出：f"测试20: 2+3*4={j}"

    // 21. 括号优先级
    设 k = (2 + 3) * 4
    执行 输出：f"测试21: (2+3)*4={k}"

    // 22. 复杂复合表达式
    设 l = 10 - 3 * 2 + 8 / 4
    执行 输出：f"测试22: 10-3*2+8/4={l}"

    // 23. 逻辑非 !
    定义 变量：布尔 not_test = !(1 == 2)
    判断 not_test：
        执行 输出："测试23: !False 为真"
    。。

    // 24. 比较表达式用于判断
    判断 10 > 5：
        执行 输出："测试24: 直接比较表达式判断通过"
    。。

    // 25. 条件变量参与比较
    设 m = 7
    判断 m > 3 且 m < 10：
        执行 输出："测试25: 复合条件判断通过"
    。。

    执行 输出："=== 运算符测试通过 ==="
。。

引用 模块：标准库-输入输出-输出 为 输出

// ========== 结构体测试 ==========

定义结构坐标：
    x：小数，
    y：小数，
    z：小数
。。

定义结构人员：
    年龄：整数，
    身高：小数
。。

@声明 入口
定义 方法 测试结构（）返回 无：
    // 1. 构造结构体字面量
    设 原点 = 构造坐标：
        x：0.0，
        y：0.0，
        z：0.0
    。。
    执行 输出：f"测试1: 原点=({原点->x},{原点->y},{原点->z})"

    // 2. 构造非零值结构体
    设 点1 = 构造坐标：
        x：1.5，
        y：2.5，
        z：3.5
    。。
    执行 输出：f"测试2: 点1=({点1->x},{点1->y},{点1->z})"

    // 3. 结构体字段访问
    设 px = 点1->x
    执行 输出：f"测试3: 点1->x={px}"

    // 4. 结构体字段赋值
    设 点2 = 构造坐标：
        x：10.0，
        y：20.0，
        z：30.0
    。。
    设 点2->x = 99.9
    执行 输出：f"测试4: 点2->x修改后={点2->x}"

    // 5. 使用为赋值结构体字段
    设 点3 = 构造坐标：
        x：1.0，
        y：2.0，
        z：3.0
    。。
    点3->y 为 88.8
    执行 输出：f"测试5: 点3->y修改后={点3->y}"

    // 6. 多结构体类型
    设 人 = 构造人员：
        年龄：25，
        身高：175.5
    。。
    执行 输出：f"测试6: 人员年龄={人->年龄}，身高={人->身高}"

    // 7. 结构体字段参与计算
    设 距离 = 点1->x + 点1->y + 点1->z
    执行 输出：f"测试7: 点1坐标和={距离}"

    // 8. 令前缀形式字段赋值
    令 点2->z = 55.5
    执行 输出：f"测试8: 点2->z={点2->z}"

    // 9. 所有字段求和
    设 total = 点2->x + 点2->y + 点2->z
    执行 输出：f"测试9: 点2坐标和={total}"

    执行 输出："=== 结构体测试通过 ==="
。。

引用 模块：标准库-输入输出-输出 为 输出

// ========== 对象测试 ==========

定义对象向量：
结构：
    x：小数，
    y：小数
构造方法（x：小数，y：小数）：
    令 当前->x = x
    令 当前->y = y
公共成员：
    定义方法相乘（另一个向量：向量）返回 小数：
        返回 当前->x * 另一个向量->x + 当前->y * 另一个向量->y
    。。
    定义方法获取X（）返回 小数：
        返回 当前->x
    。。
    定义方法获取Y（）返回 小数：
        返回 当前->y
    。。
    定义方法相加（另一个向量：向量）返回 向量：
        设 newX = 当前->x + 另一个向量->x
        设 newY = 当前->y + 另一个向量->y
        返回 创建向量（newX，newY）
    。。
。。

定义对象累加器：
结构：
    数值：整数
构造方法（初始值：整数）：
    令 当前->数值 = 初始值
公共成员：
    定义方法递增（）返回 整数：
        令 当前->数值 = 当前->数值 + 1
        返回 当前->数值
    。。
    定义方法获取（）返回 整数：
        返回 当前->数值
    。。
    // 私有方法（只能内部调用）
私有成员：
    定义方法内部加倍（）返回 整数：
        返回 当前->数值 * 2
    。。
。。

@声明 入口
定义 方法 测试对象（）返回 无：
    // 1. 创建对象
    设 向量1 = 创建向量（10.0，15.0）
    设 向量2 = 创建向量（10.0，10.0）
    执行 输出："测试1: 对象创建成功"

    // 2. 对象方法调用
    设 点积 = 向量1->相乘（向量2）
    执行 输出：f"测试2: 向量点积={点积}"

    // 3. 多次方法调用
    设 x1 = 向量1->获取X（）
    设 y1 = 向量1->获取Y（）
    执行 输出：f"测试3: 向量1=({x1},{y1})"

    // 4. 方法返回对象
    设 向量3 = 向量1->相加（向量2）
    设 x3 = 向量3->获取X（）
    设 y3 = 向量3->获取Y（）
    执行 输出：f"测试4: 向量3=({x3},{y3})"

    // 5. 累加器对象
    设 cnt = 创建累加器（0）
    设 v1 = cnt->递增（）
    设 v2 = cnt->递增（）
    设 v3 = cnt->获取（）
    执行 输出：f"测试5: 累加器递增3次={v3}"

    // 6. 方法调用作为参数
    设 向量4 = 创建向量（1.0，2.0）
    设 v4 = 向量4->获取X（） + 向量4->获取Y（）
    执行 输出：f"测试6: 向量4坐标和={v4}"

    执行 输出："=== 对象测试通过 ==="
。。

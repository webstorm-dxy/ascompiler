引用 模块：标准库-输入输出-输出 为 输出

// ========== 布尔字面量测试 ==========

@声明 入口
定义 方法 测试布尔字面量（）返回 无：
    // 1. 布尔字面量 真
    设 b1 = 真
    执行 输出：f"测试1: 真={b1}"

    // 2. 布尔字面量 假
    设 b2 = 假
    执行 输出：f"测试2: 假={b2}"

    // 3. 布尔变量在条件中使用
    判断 b1：
        执行 输出："测试3: 真作为条件通过"
    。。

    // 4. 布尔字面量直接作为条件
    判断 真：
        执行 输出："测试4: 真直接作为条件通过"
    。。

    // 5. 判断 假 不走分支
    定义 可变 变量：整数 flag = 0
    判断 假：
        flag = 1
    。。
    执行 输出：f"测试5: flag应为0，实际={flag}"

    // 6. 比较运算产生布尔值
    设 b3 = 5 > 3
    判断 b3：
        执行 输出："测试6: 比较产生真通过"
    。。

    // 7. 逻辑运算产生布尔值
    设 b4 = 真 且 假
    判断 b4 == 假：
        执行 输出："测试7: 真且假=假 通过"
    。。

    // 8. 逻辑或运算
    设 b5 = 真 或 假
    判断 b5 == 真：
        执行 输出："测试8: 真或假=真 通过"
    。。

    // 9. 取反运算作用于布尔变量
    设 b6 = !真
    判断 b6 == 假：
        执行 输出："测试9: !真=假 通过"
    。。

    // 10. 取反运算作用于假
    设 b7 = !假
    判断 b7 == 真：
        执行 输出："测试10: !假=真 通过"
    。。

    // 11. 布尔变量作为选择语句目标
    当前 b1：
        取 真：
            执行 输出："测试11: 当前选择真分支通过"
        取 假：
            执行 输出："测试11: 不应该走这个分支"
     。。

    // 12. 循环条件使用布尔变量
    定义 可变 变量：整数 count = 0
    循环条件 b1：
        count = count + 1
        判断 count >= 1：
            设 b1 = 假
        否则：
            执行 输出："循环继续"
        。。
    。。
    执行 输出：f"测试12: count={count}"

    // 13. 返回布尔类型
    设 b8 = 获取真值（）
    判断 b8 == 真：
        执行 输出："测试13: 函数返回布尔真通过"
    。。

    执行 输出："=== 布尔字面量测试通过 ==="
。。

定义 方法 获取真值（）返回 布尔：
    返回 真
。。

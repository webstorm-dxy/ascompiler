引用 模块：标准库-输入输出-输出 为 输出

// ========== 变量声明与赋值测试 ==========

@声明 入口
定义 方法 测试变量（）返回 无：
    // 1. 完整声明：指定类型，无初始化，再赋值
    定义 变量：整数 a
    a = 42
    执行 输出：f"测试1: a={a}"

    // 2. 完整声明：指定类型，带初始化
    定义 变量：整数 b = 100
    执行 输出：f"测试2: b={b}"

    // 3. 可变变量声明
    定义 可变 变量：整数 c = 10
    c = c + 5
    执行 输出：f"测试3: c={c}"

    // 4. 设（let）简化声明
    设 d = 7
    执行 输出：f"测试4: d={d}"

    // 5. 令（let 的等价形式）
    令 e = 3
    执行 输出：f"测试5: e={e}"

    // 6. 设 + 为 赋值
    设 f 为 99
    执行 输出：f"测试6: f={f}"

    // 7. 可变变量重新赋值
    定义 可变 变量：整数 g = 1
    g = g + 1
    g 为 g + 1
    执行 输出：f"测试7: g={g}"

    // 8. 小数类型（Double）
    定义 变量：小数 h = 3.14
    执行 输出：f"测试8: h={h}"

    // 9. 类型推断（设）
    设 i = 2.718
    执行 输出：f"测试9: i={i}"

    // 10. 字符串类型
    定义 变量：字符串 s1 = "hello"
    执行 输出：f"测试10: s1={s1}"

    // 11. 赋值运算符 = 和 为 均可用
    设 x = 10
    x 为 20
    执行 输出：f"测试11: x={x}"

    // 12. 使用设声明可变变量后进行运算再赋值
    设 y = 5
    y = y * 2 + 3
    执行 输出：f"测试12: y={y}"

    // 13. 预定义变量无初始化（必须指定类型）
    定义 变量：整数 z
    z = 100
    执行 输出：f"测试13: z={z}"

    // 14. 通过比较表达式获得布尔值
    定义 变量：布尔 is_true = 1 == 1
    判断 is_true：
        执行 输出："测试14: 布尔值 True 判断通过"
    。。

    // 15. 布尔取反
    定义 变量：布尔 is_false = !is_true
    判断 is_false：
        执行 输出："测试15: 不应执行"
    否则：
        执行 输出："测试15: 布尔值 False 判断通过"
    。。

    执行 输出："=== 变量测试通过 ==="
。。
